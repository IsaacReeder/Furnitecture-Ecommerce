import React from "react";

import "./DrawerToggleButton.css";

const DrawerToggleButton = (props) => (
  <div style={{ color: "white" }} onClick={props.click}>
    cart
  </div>
);

export default DrawerToggleButton;
